function setup() {
  createCanvas(windowWidth, windowHeight);
  background(121, 223,33);
  // line();
  rect(100-frameCount, 100+frameCount, 200,100);
  // ellipse(x, y, w, [h])
  // quad(x1, y1, x2, y2, x3, y3, x4, y4)
  // fill(v1, [v2], [v3], [a])
  // stroke(v1, [v2], [v3], [a])
  // strokeWeight(weight,height,windowHeight,windowWidth,mouseX,mouseY)
}

function draw() {
}
